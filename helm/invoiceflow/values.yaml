namespace: invoiceflow

backend:
  image:
    repository: your_dockerhub_username/invoicebackend
    tag: v2
    pullPolicy: Always
  port: 5000

frontend:
  image:
    repository: your_dockerhub_username/invoicefrontend
    tag: v2
    pullPolicy: Always
  port: 80

ingress:
  enabled: true
  className: traefik

secrets:
  existingSecret: invoiceflow-secrets