apiVersion: v1
kind: Service
metadata:
  name: invoicebackend
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: invoicebackend
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}