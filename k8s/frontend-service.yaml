apiVersion: v1
kind: Service
metadata:
  name: invoicefrontend
  namespace: invoiceflow
spec:
  type: NodePort
  selector:
    app: invoicefrontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30081